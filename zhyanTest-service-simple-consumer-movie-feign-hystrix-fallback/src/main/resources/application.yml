server:
  port: 8010
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      instance:
        prefer-ip-address: true
spring:
  application:
    name: zhyanTest-consumer-movie
feign:
  hystrix:
    enabled: true
